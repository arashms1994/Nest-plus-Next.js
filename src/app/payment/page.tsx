import PaymentCard from "@/components/home-components/payment/PaymentCard";
import React from "react";

const page = () => {
  return (
    <div>
      <PaymentCard />
    </div>
  );
};

export default page;
